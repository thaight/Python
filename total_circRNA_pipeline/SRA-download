prefix=$1
outpath=$2

echo Starting Download of: "$prefix"
echo fasterq-dump "$prefix" -O "$outpath""$prefix"/raw
echo Saving File in: "$outpath""$prefix"/raw
mv "$outpath"SRA-download_"$prefix".sh "$outpath""$prefix"/scripts

